version: '3.8'

services:
  app:
    build: .
    container_name: ancient-tree-analysis
    ports:
      - "3001:3001"
    environment:
      - NODE_ENV=production
      - PORT=3001
      - DB_PATH=/app/data/trees.db
    volumes:
      - ./data:/app/data
    restart: unless-stopped
